import 'package:flutter/material.dart';

/// This is for creating the dark and light mode toggle button.
///   ..and here is, at first the Value Notifier for dark mode is false, means the app is
///   at light mode.

ValueNotifier<bool> isDarkModeNotifier = ValueNotifier(false);